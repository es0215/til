apiVersion: v1
kind: Service
metadata:
  name: gke-base-app
  labels:
    app: gke-base-app
spec:
  type: ClusterIP
  selector:
    app: gke-base-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
